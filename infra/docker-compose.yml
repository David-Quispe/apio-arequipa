services:
  db:
    image: postgis/postgis:16-3.4
    environment:
      POSTGRES_USER: apio
      POSTGRES_PASSWORD: apio
      POSTGRES_DB: apio
    ports:
      - "5432:5432"
    volumes:
      - apio_pgdata:/var/lib/postgresql/data

volumes:
  apio_pgdata:
